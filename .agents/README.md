# Development workspace

This directory contains repository-internal material that is not part of the installed library or source distribution.

- `tests/`: unit, integration, documentation, and packaging tests
- [`HARDWARE_TESTS.md`](HARDWARE_TESTS.md): ordered physical-device acceptance checklist, safety gates, and cleanup requirements

Run the configured suite from the repository root with `python -m pytest`.
