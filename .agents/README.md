# Development workspace

This directory contains repository-internal material that is not part of the
installed library or source distribution.

- `tests/`: unit, integration, documentation, and packaging tests

Run the configured suite from the repository root with `python -m pytest`.
